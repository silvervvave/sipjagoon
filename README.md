# 스와이프! 중세 생존기 — 리팩터링 변경사항

간단한 설명:
- `life of medievel.html`의 인라인 스타일과 길게 포함된 외부 라이브러리 번들을 제거하고 구조를 정리했습니다.
- 스타일은 `styles.css`로 분리되었습니다.
- 스크립트는 `app.js`로 분리되었습니다.

실행 방법:
1. 로컬에서 브라우저로 `life of medievel.html` 파일을 열어 확인하세요.

변경 제안(옵션):
- `app.js`를 모듈화하고 이벤트 리스너로 교체하여 인라인 `onclick` 제거
- 반응형 레이아웃 개선 및 접근성(aria) 속성 추가
- 빌드 파이프라인이나 패키지 관리가 필요하면 알려주세요.
